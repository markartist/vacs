import { ModulePlaceholder } from "../../../components/module-placeholder";

export default function PromptsPage() {
  return (
    <ModulePlaceholder
      title="Prompts"
      description="Prompt registry, versioning controls, and release notes for channel templates will live in this module."
    />
  );
}
